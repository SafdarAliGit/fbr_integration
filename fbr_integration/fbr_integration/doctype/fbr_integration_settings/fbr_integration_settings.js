

frappe.ui.form.on("FBR Integration Settings", {
	refresh(frm) {
	//   const options = [
	// 	{ label: "Sales Invoice", value: "1" },
	// 	{ label: "Purchase Invoice", value: "2" },
	// 	{ label: "Debit Note",       value: "3" }
	//   ];
  
	//   // Apply the options to the invoice_type select field
	//   frm.set_df_property("invoice_type", "options", options);
	//   frm.refresh_field("invoice_type");
  
	//   // If there's already a stored value, ensure it matches our options
	//   if (frm.doc.invoice_type) {
	// 	const existing = options.find(o => o.value === frm.doc.invoice_type);
	// 	if (existing) frm.set_value("invoice_type", existing.value);
	//   }

	}
  });
  